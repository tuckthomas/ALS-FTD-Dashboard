# Gemini CLI specific configuration
# Source this file before running gemini, e.g.:
# export $(cat .env.gemini | xargs) && gemini

# Inherit/Duplicate values from main environment where appropriate
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DATABASE=

# Specific Agent User
POSTGRES_USER=
POSTGRES_PASSWORD=
